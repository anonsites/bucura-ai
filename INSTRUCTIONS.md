### STYLING RULES:

```css
## 1 NEVER style global HTML tags (p, h1, h2, div) in globals.css.
## 2 Use Tailwind CSS utility classes exclusively for all components.
## 3 Default to leading-normal instead of line-height: 1.7 to support long words.
## 4 Use the Text component (located in /components/ui/Text.tsx) for all paragraph text.
## 5 don't use gray paragraph, let text be bright

---